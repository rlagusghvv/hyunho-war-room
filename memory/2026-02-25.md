
## 2026-02-25 14:42 KST — Durable Notes
- 운영 고정 이슈: `hotfix/go-live-docs-20260214` pull/rebase/reset 후 `/app`, `/econ` Flutter 정적 번들이 자주 base-href(`/`)로 되돌아가 흰 화면 재발. 재발 방지 실무 규칙은 pull 직후 반드시 두 앱 재빌드 고정:
  - `/app`: `flutter build web --release --base-href /app/ --pwa-strategy=none`
  - `/econ`: `flutter build web --release --base-href /econ/ --pwa-strategy=none`
  - 이후 `public/app`, `public/econ` 동기화 + 서버 재시작 + 외부 URL 200 확인 전에는 완료 보고 금지.
- 사용자 보고 고정 선호 재확인: 완료 보고는 항상 한국어 + (1) 커밋 해시 (2) 반영/배포 여부 (3) 바로 확인 링크(URL) 포함. 플레이스홀더 URL/로컬 URL 보고 금지.
- 투자삼(트레이더봇) 장애 핵심: 같은 토큰 공유 또는 경로 불명확으로 응답/체결 알림 꼬임 반복. 직접 송신 테스트는 성공했지만 체결 이벤트 파이프라인 코드 위치는 `tesla-info/_repo`가 아님. 별도 서비스/경로 확인 필요.
- 경제탐험대 진행 핵심: 콘텐츠/UX 확장 다수 반영(튜토리얼+샘플문제, 오답노트+복습, 데일리/주간 미션, 마이룸 편집 개선, 아이템 AI 자산 반영). 그러나 자산 시트 반영 시 잘림/초소형 아티팩트가 반복되어 "아이템ID→실제 PNG 매핑" 점검과 크롭 품질 검증 필수.
- 버전 식별 요구 고정: 사용자 요청으로 UI 태그 `ui-YYYY.MM.DD-rN`을 계속 증가시켜 반영 여부를 즉시 확인 가능하게 유지.
- 쿠팡코끼리 재발 방지 고정: 서버 반영/재시작 확인은 Desktop 추정 경로가 아니라 LaunchAgent 실제 작업경로(`/Users/kimhyunhomacmini/.openclaw/workspace/coupang-automation`) 기준으로 수행. 완료 보고 전 `http://127.0.0.1:3000/health`와 `https://app2.splui.com/health` 둘 다 200 필수.
