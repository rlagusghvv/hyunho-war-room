## 18:01 KST memory flush (durable)
- OpenClaw Telegram DM access hardened to allowlist only for 현호 (8563902066), including youngsam account-level allowFrom; gateway config applied/restarted.
- Heartbeat spam reduced by setting default heartbeat cadence (30m) with active window 09:00-23:00 Asia/Seoul.
- tesla-info speed camera dataset refreshed and verified: fileData path serving 정상 (`/api/data/speed_cameras_kr` count 39,735; datasets 126). OpenAPI/fileData outputs both archived separately.
- Mac mini resilience improved: launchd watchdogs/diagnostics installed for Colima + OpenClaw health recovery.
- Telegram bridge stabilized, including better 5xx UX handling and multimodal image relay path for 트레이더삼.
- Notion integration and dashboard/project DB automation scripts built; known API constraints documented (share requirement, root-parent limitations, view automation limits).
- Coupang/Couplus pipeline significantly advanced:
  - Approval-first 방향으로 전환 (`requireApproval/confirm` 게이트 도입 중)
  - 미리보기(웹/모바일) + 실행 경로 동시 수정
  - 카테고리 템플릿/스키마 캐시/실패학습 테이블 도입
  - 물티슈 실전 카테고리 76872 + unitType=PIECE 안정 조합 확인
  - create 실패 재시도/필수속성 자동주입 로직 추가
  - 실패 알림 크론 + listing_attempts 기록 보강
- Domeggook `getItemView` 상세 원문을 쿠팡 상세(contentText)로 반영하는 강화 작업 진행 중.
- 미해결 핵심 TODO:
  1) `/api/upload/execute` 승인 게이트 E2E 검증 완료
  2) Domeggook 상세 반영 품질 점검(3~5건)
  3) `uploaded_products` upsert 전 경로 100% 강제
  4) 카테고리 80649 옵션/단위 정합성 해결
  5) cloudflared token 노출 건 rotation 완료 재확인
- 보안 원칙 재확인: .env/토큰/키는 커밋·로그·채팅 노출 금지.
