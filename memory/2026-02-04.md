# 2026-02-04

## Couplus / Coupang automation

### Key fixes (Coupang upload)
- Image hosting fallback:
  - Found Coupang image upload endpoint returned 404 PRECONDITION_FAILED for this account.
  - Switched default to public hosting fallback (Cloudflare Tunnel / imageProxyBase) by setting `useCoupangImageUpload` default to 0.
- `/couplus-out` static hosting bug:
  - Fixed `server.js` to serve `/couplus-out` from `process.cwd()/out` (was pointing to unrelated hardcoded path), resolving `image_host_unreachable`.
- Upload success display:
  - Fixed `/api/upload/execute` to propagate pipeline `ok` so UI doesn't show success when pipeline fails.
- Delivery company code:
  - Coupang create API returned HTTP 200 but body code ERROR when `deliveryCompanyCode` missing or invalid.
  - Passed `settings.coupangDeliveryCompanyCode` into payload (deliveryFree policy), and set to CJGLS.
- Option constraints:
  - Coupang option value max length 30 chars; added truncation with ellipsis.
  - Treated Coupang create body `code !== SUCCESS` as pipeline failure (`coupang_create_failed`).

### Shipping fee policy
- Parsed shipping fee from vendor page text into `draft.shippingFee` (0=free, >0=paid amount, -1=paid but unknown).
- Pricing rule changed:
  - Initially +2500 for paid shipping.
  - Updated to add actual shipping fee amount.
  - If paid-but-unknown (`-1`) => fail upload with `shipping_fee_unknown`.

### Dedupe + force reupload UX
- Added sqlite table `uploaded_products` (unique user_id + source_url).
- Block duplicates by source URL returning `duplicate_product` + existing sellerProductId + product URL.
- Added `force=1` to bypass dedupe.
- UI improvement: when duplicate occurs, show actions: open existing / force reupload.

### Cloudflare / connectivity
- Started trycloudflare tunnel exposing localhost:3000.
- Updated user settings `imageProxyBase` to tunnel base.

## Push notifications

### PWA Web Push
- Added web push (VAPID) support: db `push_subscriptions`, endpoints:
  - GET `/api/push/public-key`
  - POST `/api/push/subscribe`, `/api/push/unsubscribe`
- Service worker `push` handler shows notifications.

### Native APNs (TestFlight)
- Located mobile app source: `coupang-automation/couplus_mobile` (Flutter).
- Bundle ID found: `com.hyunho.coupelephant.app`.
- Added APNs support:
  - iOS AppDelegate requests permission, registers remote notifications, passes device token to Flutter via MethodChannel `couplus/push`.
  - Flutter `PushTokenService` sends token to server `/api/apns/register` after login cookie exists.
- Server APNs support:
  - Added db table `apns_tokens`, endpoints:
    - POST `/api/apns/register`, `/api/apns/unregister`
  - Added `notifyUser()` that sends both Web Push + APNs (best-effort).
  - APNs config via env:
    - APNS_TEAM_ID=FCHA9MNH8C
    - APNS_KEY_ID=87GSWAQ5P2
    - APNS_BUNDLE_ID=com.hyunho.coupelephant.app
    - APNS_P8_PATH=data/apns_auth_key.p8
  - Stored p8 to `coupang-automation/data/apns_auth_key.p8` (chmod 600).

### Build + TestFlight upload
- Bumped Flutter build number: `0.1.0+15`.
- Fixed build error: removed redundant `UNUserNotificationCenterDelegate` conformance.
- Built ipa and uploaded successfully with `xcrun altool`.
  - Upload succeeded, Delivery UUID: 22f7b74c-68f9-4623-a9c6-73a8c5961888.

## Notes / request
- User requested: "앞으로는 한번 찾아보고 말해줘" and "작업했던 내용 잊지 않게 기록".
